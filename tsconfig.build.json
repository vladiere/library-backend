{
  "extends": "./tsconfig.json", // Extend your main tsconfig.json
  "compilerOptions": {
    "outDir": "./build", // Specify the output folder for the build
    "removeComments": true, // Remove comments from the compiled code
    "declaration": true, // Generate .d.ts declaration files
    "sourceMap": false, // Disable source map generation
    "inlineSources": false, // Disable including source code in sourcemaps
    "emitDecoratorMetadata": true, // Emit decorator metadata
    "strict": true // Enable all strict type-checking options
    // You can add other production-specific options here
  },
  "include": ["src/**/*.ts", "src/**/*.json", "src/**/*"], // Include TypeScript source files
  "exclude": ["node_modules"] // Exclude node_modules folder
}
